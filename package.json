{
  "name": "telegram-work-logger",
  "version": "1.0.0",
  "description": "A Telegram bot for logging daily work hours with MongoDB storage",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "moment": "^2.29.4",
    "mongoose": "^8.17.0",
    "node-telegram-bot-api": "^0.64.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
